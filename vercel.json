{
  "version": 2,
  "buildCommand": "cd apps/web && NEXT_TELEMETRY_DISABLED=1 npm install && NEXT_TELEMETRY_DISABLED=1 npm run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
